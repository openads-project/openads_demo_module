name: docs

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
    branches:
      - contribution-graph
  workflow_dispatch:

jobs:
  docs:
    uses: openads-project/openads-dev-environment/.github/workflows/docs.yml@contribution-graph
    secrets: inherit
