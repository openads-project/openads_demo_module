name: docs

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  workflow_dispatch:

jobs:
  docs:
    uses: OpenAutomatedDriving/dev-environment/.github/workflows/docs.yml@submodule # TODO: revert to main
    secrets: inherit
