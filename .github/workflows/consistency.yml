name: consistency

on:
  push:
  workflow_dispatch:

jobs:
  consistency:
    uses: oads-org/openads_ros2_demo_repository/.github/workflows/consistency.yml@submodule # TODO: revert to main
    secrets: inherit
